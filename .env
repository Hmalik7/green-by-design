

# Frontend Environment Variables
# Save this as: client/.env (or in your src folder if that's where your frontend is)

# API Base URL for your Express backend
VITE_API_BASE_URL=http://localhost:8080

# Note: Only VITE_ prefixed variables are exposed to the browser
# Don't put any secrets here - this file can be seen by users
